name: app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  record: ^5.0.5
  audioplayers: ^6.0.0
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  sensors_plus: ^5.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
